{
  "version": 3,
  "sourceRuntime": "bridge-compat-host",
  "units": [
    {
      "id": "message-intake-and-projection",
      "buildOrder": 6,
      "cutoverAfter": [],
      "modules": ["approval-cards", "dida-projection", "direct-command-workflow", "focus-intake", "owner-engagement", "notification-digest"],
      "targetModules": ["feishu-native-ingress", "message-intake-native", "feishu-workflow-effects", "feishu-context-effects", "feishu-focus-discovery-effects", "intake-interaction-effects", "dsh-reasoning-effects", "dsh-conversation-effects", "dida-task-effects", "dida-projection-effects"],
      "stateOwner": "managed compatibility handoff state.json",
      "cutoverBoundary": "Freeze one checkpoint, stop all five compat Feishu streams, import pending message/card/projection state, then start the five native streams exactly once.",
      "rollback": "Stop every native stream before restoring the frozen compat checkpoint and bridge-compat process.",
      "requiresMaintenanceWindow": true
    },
    {
      "id": "dida-maintenance",
      "buildOrder": 2,
      "cutoverAfter": ["message-intake-and-projection"],
      "modules": ["dida-maintenance"],
      "targetModules": ["dida-maintenance-native", "dida-task-effects", "feishu-workflow-effects"],
      "stateOwner": "managed compatibility handoff state.json",
      "cutoverBoundary": "Disable compat overdue and completed-task timers, migrate notification fingerprints and cleanup cursor, then enable the native scheduled plugin.",
      "rollback": "Disable the native timer before restoring migrated fingerprints and re-enabling compat timers.",
      "requiresMaintenanceWindow": true
    },
    {
      "id": "delegated-followup",
      "buildOrder": 5,
      "cutoverAfter": ["message-intake-and-projection"],
      "modules": ["delegated-followup"],
      "targetModules": ["delegated-followup-native", "dsh-reasoning-effects", "dida-task-effects", "dida-projection-effects", "feishu-workflow-effects"],
      "stateOwner": "managed compatibility handoff state.json",
      "cutoverBoundary": "Pause compat follow-up polling, migrate open outreach approvals and last workday checkpoint, then enable the native workflow.",
      "rollback": "Stop native polling, export unresolved approvals back to the frozen compat schema, then resume compat polling.",
      "requiresMaintenanceWindow": true
    },
    {
      "id": "xiaowei-research",
      "buildOrder": 4,
      "cutoverAfter": ["message-intake-and-projection"],
      "modules": ["xiaowei-research"],
      "targetModules": ["xiaowei-research-native", "feishu-workflow-effects", "dida-projection-effects"],
      "stateOwner": "managed compatibility handoff state.json",
      "cutoverBoundary": "Pause Xiaowei request/reply polling, migrate every pending correlation and approval, then resume through one native channel owner.",
      "rollback": "Stop native Xiaowei polling and restore only unresolved correlations to compat before resuming it.",
      "requiresMaintenanceWindow": true
    },
    {
      "id": "session-lifecycle",
      "buildOrder": 3,
      "cutoverAfter": ["message-intake-and-projection"],
      "modules": ["session-lifecycle"],
      "targetModules": ["session-lifecycle-native", "codex-session-effect-adapter", "dida-task-effects", "feishu-workflow-effects"],
      "stateOwner": "managed compatibility handoff state.json",
      "cutoverBoundary": "Pause archive/delete sweeps, migrate session ownership, Dida completion links and retry deadlines, then enable native cleanup.",
      "rollback": "Stop native cleanup before restoring remaining session records; never replay already-completed deletes.",
      "requiresMaintenanceWindow": true
    },
    {
      "id": "collaboration-learning",
      "buildOrder": 1,
      "cutoverAfter": ["message-intake-and-projection"],
      "modules": ["collaboration-learning"],
      "targetModules": ["collaboration-learning-native"],
      "stateOwner": "managed compatibility handoff state.json",
      "cutoverBoundary": "Pause learning evaluation, import privacy-bounded observations and proposal cooldowns, then enable native policy learning.",
      "rollback": "Disable native evaluation and restore only observations newer than the frozen compat checkpoint by stable sample id.",
      "requiresMaintenanceWindow": true
    }
  ],
  "exitUnits": [
    {
      "id": "promote-native-profile",
      "modules": ["assistant-profile-composition"],
      "disposition": "reclassify-feature",
      "afterCutoverUnits": ["message-intake-and-projection", "dida-maintenance", "delegated-followup", "xiaowei-research", "session-lifecycle", "collaboration-learning"],
      "afterExitUnits": [],
      "verification": "Cordis profile contains no ASSISTANT_RUNTIME=compat gate; every mounted local plugin binding and runtime owner passes architecture:check and a supervised restart.",
      "rollback": "Restore the last compatibility-aware profile before restarting the compatibility host.",
      "requiresMaintenanceWindow": true
    },
    {
      "id": "retire-compat-host",
      "modules": ["bridge-compat-host"],
      "disposition": "remove",
      "afterCutoverUnits": ["message-intake-and-projection", "dida-maintenance", "delegated-followup", "xiaowei-research", "session-lifecycle", "collaboration-learning"],
      "afterExitUnits": ["promote-native-profile"],
      "verification": "No process, profile, source import, runtime dependency, state writer or deployment entry references bridge-compat; one native consumer owns each Feishu stream after restart.",
      "rollback": "Stop every native consumer, restore the frozen compatibility release and state checkpoint, then start compatibility consumers exactly once.",
      "requiresMaintenanceWindow": true
    },
    {
      "id": "retire-takeover-readiness",
      "modules": ["takeover-readiness"],
      "disposition": "remove",
      "afterCutoverUnits": ["message-intake-and-projection", "dida-maintenance", "delegated-followup", "xiaowei-research", "session-lifecycle", "collaboration-learning"],
      "afterExitUnits": ["retire-compat-host"],
      "verification": "Control surfaces read open OperationalReadinessReport providers and no code or asset reads feature-parity.json.",
      "rollback": "Restore the adapter and parity asset only together with the compatibility rollback release.",
      "requiresMaintenanceWindow": false
    },
    {
      "id": "retire-legacy-state-tooling",
      "modules": ["legacy-state-tooling"],
      "disposition": "remove",
      "afterCutoverUnits": ["message-intake-and-projection", "dida-maintenance", "delegated-followup", "xiaowei-research", "session-lifecycle", "collaboration-learning"],
      "afterExitUnits": ["retire-compat-host"],
      "verification": "No production read or write path uses compatibility state.json; rollback retention has elapsed and native checkpoints cover every migrated queue.",
      "rollback": "Keep the frozen content-addressed handoff outside live write paths until the declared rollback retention expires.",
      "requiresMaintenanceWindow": false
    },
    {
      "id": "retire-takeover-evidence",
      "modules": ["takeover-evidence"],
      "disposition": "remove",
      "afterCutoverUnits": ["message-intake-and-projection", "dida-maintenance", "delegated-followup", "xiaowei-research", "session-lifecycle", "collaboration-learning"],
      "afterExitUnits": ["retire-takeover-readiness", "retire-legacy-state-tooling"],
      "verification": "The compatibility host, parity adapter and legacy state tooling are absent; final production replay, rollback drill and architecture audit are stored as release evidence.",
      "rollback": "Restore this evidence tooling only on the isolated rollback branch; it must not return to the native product runtime.",
      "requiresMaintenanceWindow": false
    }
  ]
}
