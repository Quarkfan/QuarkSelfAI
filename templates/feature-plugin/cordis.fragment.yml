- id: feature-id
  name: '@quarkfan/quark-self-ai/feature-id'
  # New features start inactive. Replace the gate with the exact ownership or
  # maintenance-window condition; never default a new consumer or writer on.
  disabled: !!js process.env.QUARK_FEATURE_ID_ENABLED !== 'true'
  config:
    enabled: true
