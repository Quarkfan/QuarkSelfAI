# 接管前受控演练（2026-08-22）

状态：已获常东旭批准；自然语言策略步骤已完成，其余步骤执行中

## 批准请求

- 飞书会话：常东旭与助手机器人私聊。
- 卡片消息 ID：`om_x100b67ae801faca4b14c0748f31a0e9`。
- 发送时间：2026-08-22 22:06:37（北京时间）。
- 交互：Card 2.0 表单输入框 + “批准并提交”按钮 + “暂不演练”按钮。
- 幂等键：`quark-takeover-rehearsal-20260822-v1`。

卡片提交由现网 bridge 的通用 `prompt` 表单链路送回同一个持久 controller session；拒绝按钮只写入确认
回执。没有收到明确批准前，以下步骤均不得执行。

## 批准回执

- 现网 card event 计数从 10 增至 11，且无重复事件。
- owner 校验后的 controller queue 收到同一 session 的表单文本：`批准按卡片所列边界执行接管前受控演练。`
- 本次批准仅覆盖下列四项固定合成演练，不构成接管、现网消费者切换或普通业务写入授权。

## 已完成：自然语言策略

- 命令：`npm run rehearse:policy`。
- 状态：临时目录中的隔离 SQLite；运行结束后删除，不存在生产策略残留。
- 固定 policy ID：`6f5192d4-a7a7-5c12-a6ea-202608220001`。
- 样本：20 条固定合成样本，仅 1 条命中；紧急误抑制为 0。
- 结果：相同草案复用 revision 1；变更草案生成 revision 2；获批激活 revision 2；获批回滚到 revision 1。
- 外部写入：0；未读取或输出业务消息正文；未触碰旧 bridge。
- 契约：激活与回滚均拒绝 `ownerConfirmed=false`，Web 控制面也要求精确 policy ID/revision 和确认字段。

## 已完成：可见 Codex 任务

- task ID：`01a029d8-c703-7d83-9c18-ecf13504a544`；host：`local`。
- 唯一标题：`QuarkSelfAI-受控演练-20260822-可见任务`。
- 模型：`gpt-5.6-sol`；推理强度：`medium`；projectless 隔离目录，不挂载业务项目。
- 第一次固定回执：`QUARK_VISIBLE_THREAD_OK`；同一 task 续接固定回执：`QUARK_VISIBLE_THREAD_RESUME_OK`。
- `list_threads` 已证明该标题和 task 出现在桌面任务列表；续接结束后已归档，避免测试任务占用普通列表。
- 未读取业务文件、未调用外部工具、未产生外部写入。后续按“自建任务归档一周后删除”的统一生命周期清理，
  不使用需要交互终端的删除命令。

## 获批后执行范围

1. **自然语言策略**：使用只匹配固定合成短语的无害规则完成草案、样本模拟、精确 revision 批准、激活
   和回滚；不得改变普通业务消息行为。
2. **可见 Codex 任务**：创建一个标题带唯一演练标识的任务，模型固定 `gpt-5.6-sol`、推理强度
   `medium`，只处理固定合成文本；验证左侧可见、续接和标题后按自动任务生命周期清理。
3. **执行器兜底**：在隔离进程中令 Claude 通道发生明确的启动层基础设施失败，确认前一 run 完全退出后
   才由真实 Codex 处理固定合成文本；不得把确定性业务错误误路由到第二个模型。
4. **故障与恢复通知**：在复制状态和隔离 connector 中制造可恢复连接故障，验证持久化、去重、北京时间、
   恢复后补发和最终健康；不得断开或重启现网飞书消费者。

## 明确排除

- 不创建或修改任何滴答任务。
- 不联系任何其他人，不在群内发言。
- 不停止、重启或修改 `com.blacklake.codex-lark-bridge`。
- 不读取普通业务文件；模型输入仅包含固定合成文本。
- 不设置 `TAKEOVER_CONFIRMED=true`，不启动新消息或卡片消费者。

## 每步通过条件

- 证据包含请求通道、实际执行通道、失败阶段、失败原因、session/thread ID 和幂等标识，但不包含 token、
  本地文件正文或业务消息正文。
- 任一步出现非预期外部写入、双执行、重复通知或无法恢复的状态，立即停止后续步骤；隔离状态直接废弃，
  现网服务保持原状。
- 全部完成后更新 `config/feature-parity.json`；只有证据足够的能力才标记 complete。
