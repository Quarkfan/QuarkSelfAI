# QuarkSelfAI 正式切换记录（2026-08-23）

## 决策

常东旭明确要求立即切换，并接受在运行中继续补齐证据；架构完整和可扩展性优先。依据 ADR 0004，切换未
修改 `dida-projection` 与 `shadow-collaboration` 的 partial 状态，而是精确记录为本次 accepted risk。

## 最终交接

- 旧 LaunchAgent `com.blacklake.codex-lark-bridge` 在无 Claude/Codex 子进程、controller queue=0、
  pending focus=0 时收到 SIGTERM 并清洁退出；两条 lark event consumer 均返回 exit 0。
- 冻结状态 SHA-256：`9c82538f42f05e8e82efd045a2b47a6d0f765e13c0374f8d7f5f434ee400ce98`。
- 内容寻址 handoff：`139a5aab86f473df6b62`；源配置和状态只读，未覆盖旧文件。
- 迁移保留 3 个待确认调研；queue、pending focus、pending Xiaowei 均为 0。
- 预检返回 `ready=true`、`acceptedRiskCutover=true`，所有 CLI、滴答凭证、显式 varDir 和状态结构通过。

## 启动与恢复

- 新 LaunchAgent：`com.quarkfan.quark-self-ai`。
- 首次启动发现 launchd 最小 PATH 无法满足基于 `#!/usr/bin/env node` 的 CLI。父守护因 compat worker 未
  ready 按设计退出，没有形成半健康服务，也没有恢复旧消费者造成双消费。
- 修复落在通用 LaunchAgent 模板：显式渲染非秘密 executable PATH，并增加模板测试和部署手册约束。
- 修复后父守护、DSH `feishu-assistant` profile、compat worker、消息 consumer、卡片 consumer 全部 ready；
  `/api/health` 返回 200、SQLite 健康。
- 随后执行一次受控 `launchctl kickstart -k`。新一代进程重新加载同一 SQLite 和 handoff state，两条事件
  consumer 再次 ready，证明登录态守护重启路径可恢复。

## 持续边界

- 旧 LaunchAgent plist 保留用于回滚，但服务未注册、未运行。
- Web 健康状态显示 `operationalMode=accepted-risk-cutover`，同时保持 `takeoverReady=false`，避免把证据债务
  误报为完成。
- 继续自然收集 current-schema 滴答投影并完成影子窗口；不创建测试任务补样本。
- 任何消费者级故障先停止 QuarkSelfAI，再 bootstrap 旧 LaunchAgent；不得让两者同时运行。

## 2026-08-24 持续审计

- current-schema 滴答投影严格血缘审计已达到 `115/20`：`ignored=47`、`created=9`、`unchanged=51`、
  `updated=8`，旧 schema 跳过、语义失败和重复血缘均为 0；审计未产生外部写入，也未创建测试任务。
- 无写影子窗口仍要运行到 `2026-08-27T11:52:20.935Z`。当前聚合为 156 decisions、67 matters、
  22 task snapshots、3 feedback、19 differences、40 task mutations；153/153 来源带上下文，覆盖 33 个
  会话和 23 个发送人。
- 严格影子审计仍发现 3 个 `missing-shadow-source-reference`，因此 `valid=false`、
  `readyForEvaluation=false`。该门禁继续保留；本轮不切换运行所有权，也不重复发送业务通知。
