# Capability Platform Console POC

Static, dependency-free interaction prototype for the proposed cloud-control/local-execution platform. It uses fictional, non-sensitive data and performs no network, filesystem, installation, activation or external-write operation.

Open `index.html` directly or serve this directory with a local static HTTP server. Coverage anchors are audited against `config/capability-platform-console-coverage.json`.
